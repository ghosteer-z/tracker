{
  "name": "interview-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal ledger for complete, primary-source interviews.",
  "scripts": {
    "validate": "node scripts/validate-data.js",
    "import": "node scripts/import-interview.js",
    "report": "node scripts/generate-report.js",
    "test": "node --test tests/*.test.js"
  }
}
