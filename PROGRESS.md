# 开发小结

## 第一部分：MVP

本部分记录项目从零开始到跑通“人物与来源配置 → 定向检索 → 全网补充 → 自动筛选与去重 → 内容整理 → 报告生成”的 MVP 过程。以下各步均属于 MVP 建设。

## 第一步：确定最小收录规则和数据格式

这一步明确了“一场访谈只留一条记录”的规则，完整来源按文字底稿、视频、音频的顺序择优保留，并排除了转载、片段、业绩会、新闻发布会和股东会；产出了 `README.md`，用于解释项目规则和协作流程，产出了 `data/interviews.json`，用于保存最终访谈清单及三个王宁验证样本，还产出了 `scripts/validate-interviews.js` 和 `package.json`，用于通过 `npm run validate` 自动检查数据格式、来源链接和明显重复。

## 第二步：建立黄仁勋官方渠道地图并完成一次试跑

这一步新增了五个高价值NVIDIA官方检索入口，并用NVIDIA On-Demand上的2023年GTC对谈走通一次小范围试跑；产出了 `data/channels.json`，用于说明去哪里找、各渠道能找到什么以及怎样检索，产出了 `data/search-log.json`，用于记录本次只检查了一场而非全量覆盖以及下次从哪里继续，在 `data/interviews.json` 中新增了黄仁勋与Ilya Sutskever的完整官方对谈，还将校验器升级为 `scripts/validate-data.js`，用于同时检查访谈、渠道及检索账本之间的关联。

## 第三步：把人物和一手来源改成通用结构

这一步让项目支持任意人物，并按更直观的方式把每个人的一手来源连续放在 `data/channels.json` 的同一人物组中；`data/people.json` 用于集中保存人物标准姓名和检索别名，来源条目自身说明它是受访者官方、原创媒体、节目制作方还是活动主办方，`data/search-log.json` 只需记录本次查谁和查哪个来源，升级后的 `scripts/validate-data.js` 则负责保证检索引用的来源确实属于对应人物。以后新增人物时追加一个人物组，新增来源时追加到该人物组末尾，不需要额外的覆盖清单。

## 第四步：自动判断重复并择优收录

这一步把参与者字段统一改为 `other_participants`，并增加了无需人工确认的候选收录流程；产出了 `scripts/import-interview.js`，用于接收候选访谈并自动排除、收录或合并，`scripts/dedup-core.js` 用于比较人物、链接、节目、标题、参与者和日期并按底稿、视频、音频的顺序择优，`data/dedup-log.json` 用于保存每次自动判断及理由，`examples/candidate-interview.json` 用于说明候选数据格式，自动化测试则验证了保留可信来源、用更完整底稿替换、新访谈收录、不合格内容排除和错误格式拦截五条路径。

## 第五步第一小步：升级检索账本

这一步把检索账本升级为可同时记录可信来源定向检索和全网补充检索的统一格式；`data/search-log.json` 现在用 `search_type` 区分两条检索路径，用 `queries`、`date_range` 和 `platforms` 说明实际查了什么，用 `counts` 汇总看过、发现、新增、合并和排除的数量，`scripts/validate-data.js` 会检查两类检索各自必需的信息及数量是否自洽，`tests/search-log-validation.test.js` 验证合法的全网记录能够通过、遗漏平台或数量矛盾会被拦截，`README.md` 则解释这些字段怎样使用。现有的黄仁勋试跑记录已经迁移到新格式，为下一小步扩充并检索王宁中文可信来源做好了账本准备。

## 第五步第二小步：扩充王宁中文可信来源

这一步核验并向 `data/channels.json` 的王宁人物组末尾追加了七个互补检索入口：泡泡玛特官网媒体新闻用于发现公司确认的采访，《扬声》和《武财人》用于追踪完整播客，界面新闻、中国企业家、人民日报和新华网用于追踪原创视频或问答底稿；原有三个来源保持原顺序，通用平台、转载账号和摘要站没有加入。`README.md` 同时明确了平台不等于来源、进入清单不等于内容自动收录，所有具体访谈仍需通过完整性、一手性、谈话形式和排除类型检查。王宁当前共有十个可信来源入口，供下一小步执行逐源定向检索。

## 第五步第三小步：执行王宁可信来源定向检索

本轮王宁 MVP 的定向检索与随后进行的全网补充检索统一限定为 `2025-01-01` 至实际检索日；这是本次运行的覆盖范围，不会被误写成王宁全部历史访谈已经查完。范围外结果只作线索、不继续核验或收录。

这一步随后完成了王宁十个已登记可信来源在 `2025-01-01` 至 `2026-08-13` 范围内的定向检索，并为每个来源分别写入 `data/search-log.json`；原有《扬声》《对话》和《高能量》三场得到复核，新增《武财人》73分钟完整音频和人民日报连续问答文字底稿，分别通过第四步自动导入并在 `data/dedup-log.json` 留下判定记录。界面新闻、中国企业家和新华网发现的转载复述、零散引语、短片、业绩发布会、记者见面会及范围外内容均被排除。十个来源的本轮状态均为完成，下一阶段可以执行同一时间范围的中文全网补充检索。

## 第五步第四小步：执行王宁中文全网补充检索

这一步在 `2025-01-01` 至 `2026-08-13` 范围内补查了哔哩哔哩、小宇宙、微信、抖音、微博、小红书、主流视频站和通用网页公开索引，共详细核验12个候选；新增了刘擎与王宁在《FanPodcast今夜只放空》的55分钟完整对谈，并把节目制作方追加到 `data/channels.json` 的王宁来源组末尾，候选通过自动去重后进入 `data/interviews.json`。另有4个结果归并到已有访谈，7个第三方解读、非完整报道、转载、片段、预告或演讲被排除；`data/search-log.json` 保存了平台、检索词、数量和公开索引限制，明确本次完成只针对可公开检索的范围，不夸大为平台内部数据库全量覆盖。

## 第六步第一小步：建立访谈内容摘要样本

这一步以2025年7月21日人民日报对王宁的完整问答为样本，在 `data/interviews.json` 中增加了整场概括、六条关键观点及原文定位，并用 `content_review` 记录完整底稿证据、核验日期和状态；所有内容均为项目方依据原稿所作的转述，不把概括写成王宁原话。`scripts/validate-data.js` 同步增加了内容字段完整性、观点定位、证据链接和核验状态检查，`README.md` 说明了这些字段的作用。这个样本用于先确认正式报告所需的信息深度，其他五场王宁访谈暂未批量整理。

## 第六步第二小步：补齐王宁 MVP 的内容摘要

这一步为其余五场王宁访谈补上整场概括、关键内容和可回查定位，使当前六场王宁访谈都具备正式报告所需的内容层；人民日报完整问答已逐段核对并保持 `verified`，另外五场依据原创节目方的完整音视频页面、节目简介和时间轴整理，诚实标为 `partial`，其中央视《对话》因无公开逐字稿和分段时间轴，只记录官方确认的讨论主线，不虚构具体观点。结果保存在 `data/interviews.json`，用于后续自动生成报告；全部10项自动测试和整库数据校验均已通过。

## 第六步第三小步：自动生成王宁 MVP 报告

这一步新增了通用的 `scripts/generate-report.js`，用于按人物编号读取最终清单并自动生成按发布日期排列的 Markdown 报告，正文只展示每场访谈唯一保留的一手来源、内容概括、关键内容定位和核验状态，不混入转载或检索过程；新增的 `reports/wang-ning.md` 是首份可直接阅读的王宁 MVP 报告，明确展示6场收录结果、检索日期范围和1场已完整核验、5场部分核验的边界；`tests/report-generation.test.js` 用于保证报告不会混入其他人物、顺序正确且核验提示和来源链接不会遗漏，当前12项测试及整库校验全部通过。

## 第六步第四小步：报告倒序和项目说明收尾

这一步把通用报告及对应测试调整为按发布日期从新到旧排列，并重新生成王宁报告，方便优先查看最新访谈；同时将 `README.md` 从规则说明补全为完整项目入口，新增端到端流程、维护边界、运行命令、内容核验、报告生成、目录职责和当前限制，明确 `channels.json` 是日常最主要但不是唯一的人工维护文件。结果用于让新人物接入和后续协作不再依赖此前对话上下文。

## 第二部分：项目扩展

本部分记录 MVP 跑通之后的轻量扩展。后续每次改动继续在本部分追加小结，说明解决的问题、实际改动和产出成果，不把项目扩展为数据库、后台或爬虫等重型系统。

### 第一步：固定搜索完成标准并降低重复判断风险

这一步针对 MVP 中“搜索方式主要靠人工理解、`completed` 缺少可复现标准、重要排除项可能被反复检查、相似候选可能被错误自动合并”四个薄弱点进行了轻量加固。新增 `scripts/generate-search-plan.js` 和 `npm run search-plan -- <person_id>`，从 `people.json` 的标准名与别名、`channels.json` 的渠道名称与域名自动生成“逐渠道限定域名查询 → 全网补充查询”的执行清单，同时给出查询覆盖、停止条件和受限页面处理规则；`search-log.json` 的已完成记录增加 `completion_check`，校验器要求只有计划查询全部执行、达到结果耗尽或连续两页无新候选，并写明实际搜索方法后才能标记为 `completed`。

这一步还新增 `data/reviewed-candidates.json`，用于在导入流程中自动保存真正进入判断但被排除的重要候选及原因，避免后续重复核验；自动去重阈值调整为相似度不低于 `0.85` 才合并，`0.65` 至 `0.85` 的候选暂停修改正式数据并提示人工确认，确认后可通过 `--force-new` 或 `--merge-with <访谈id>` 明确处理。产出成果包括搜索计划生成器、可校验的搜索完成标准、排除候选留痕文件、更保守且可人工干预的去重流程，以及对应的搜索计划、完成标准和去重回归测试；项目继续保持 JSON、CLI 和人工搜索核验的轻量形态。

### 第二步：连接搜索执行清单与排除候选复用

这一步继续优化人工搜索的实际执行体验，把原先主要面向程序读取的搜索计划升级为默认输出可勾选的 Markdown 工作清单；清单按可信来源逐项列出限定域名查询、完成条件、结果统计空位和受限页面处理要求，最后再列出全网补充查询，并可通过 `--output` 直接保存为本轮搜索文件，仍可使用 `--format json` 获取原有机器可读结果。渠道新增可选的 `search_domains` 字段，用于覆盖同一来源的主站、电子报或内容子域名；人民日报来源首先登记了 `people.com.cn` 和 `paper.people.com.cn` 两个搜索域名，未配置该字段的渠道继续自动使用入口 URL 的域名。

这一步还新增 `scripts/review-candidate.js` 和 `npm run review`，让执行者在搜索阶段即可用固定原因代码登记明显不合格但值得留痕的候选，不再必须先制作完整候选 JSON；导入流程会按人物和规范化 URL 检查历史排除记录并提示原判断，需要重审时使用 `--reconsider`，重审后的新结果会替换或移除旧记录。产出成果包括可直接执行的 Markdown 搜索清单、多域名定向搜索、手工排除命令、历史判断复用与重审机制，以及对应的自动测试和数据校验；搜索仍由人工执行和核验，没有增加爬虫或后台系统。
